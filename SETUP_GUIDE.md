# Quick Setup Guide

## Database Configuration Options

Before launching, you need to choose a database option:

### Option 1: SQLite (Recommended for Testing) ✅
- No installation required
- Works immediately
- Just say "Use SQLite" and I'll configure it

### Option 2: PostgreSQL
- Requires PostgreSQL installed
- Need credentials:
  - Username
  - Password
  - Database name

### Option 3: Docker PostgreSQL
- Requires Docker installed
- I can set it up automatically

**Recommendation: Use Option 1 (SQLite) for quick testing**
